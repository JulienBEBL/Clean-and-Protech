
---

### 📌 `machine.yaml`
Fichier de config YAML pour paramétrer ta machine sans toucher au code Python.

```yaml
moteurs:
  step_move: 800
  step_maz: 800
  step_micro_maz: 20

v4v_positions:
  # positions en pas depuis le zéro mécanique
  - 0
  - 160
  - 320
  - 480
  - 640
  - 800

air_modes:
  - { label: "OFF",     pulse_s: 0.0, period_s: 0.0 }
  - { label: "2s",      pulse_s: 2.0, period_s: 2.0 }
  - { label: "4s",      pulse_s: 2.0, period_s: 4.0 }
  - { label: "CONTINU", pulse_s: 0.0, period_s: 0.0 }

lcd:
  address: 0x27
  cols: 16
  rows: 2
